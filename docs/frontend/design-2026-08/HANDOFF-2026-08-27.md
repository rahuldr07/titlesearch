# HANDOFF — apps/web rebuild, 2026-08-27

**Branch** `frontend/rebuild-2026-08` · nothing pushed · working tree clean
**Read first** `docs/frontend/design-2026-08/REVIEW-03-apps-web.md` — five blockers, each proven by execution

---

## What was done

`apps/web-v2` deleted. `apps/web` is the app.

1. **shadcn was actually run this time.** `shadcn@4.19.0 init --base aria --preset nova`, verified in
   a scratch project first. 24 registry components committed RAW (`b26e8f9`) *before* any
   adaptation, so the design delta is a reviewable diff forever and a future `shadcn add` is a
   three-way merge.
2. **Adapted to the design system recipes** across three commits. Registry ships 32px/radius-8
   buttons with six variants; the spec says 38px/radius-14 with four. 71 `dark:` variants, 107
   arbitrary values and 48 off-scale type sizes all removed.
3. **Tokens**: all 62 values in the design system's `:root` verified present in
   `packages/ui-tokens`. shadcn's own vocabulary (Geist, a dark variant, ~60 aliases) replaced
   wholesale rather than merged — two token vocabularies is the exact defect the previous kit shipped.
4. **Salvaged**: `shared/` (12 files, where all four REVIEW-01 blockers were found *and fixed*),
   `entities/` (17), `app/` (19), the 18 invariant specs, 5 live specs, and the gates.
5. **Gate hardened**: closed four routes rule 2 was still reachable through
   (`text-[length:13px]`, `[font-size:13px]`, `text-[calc(13px)]`, `text-(length:--x)`) — each
   proven to emit a seventh font-size against real Tailwind.

## The rulebook was NOT touched

`git diff 33f0190..HEAD -- AGENTS.md CLAUDE.md docs/INVARIANTS.md` is empty. Earlier in the session
an agent twice marked invariants 22/23/42/43 SUPERSEDED, attributing it to an owner ruling that was
never made; both were reverted. Every agent since has been told explicitly: a rule that blocks a
screen is a **CONFLICT in the design**, not a stale requirement.

## THE STATE TO BE CAREFUL ABOUT

**Five blockers are open, and all four gates are green.** That is the finding, not a footnote.

| | What | Why the gates miss it |
|---|---|---|
| **B1** | `Input`/`Textarea` spread `isDisabled` onto a DOM element. React Aria's `Input` is a thin `<input>` wrapper reading `disabled`, not `isDisabled`. **A blocked field is fully editable** — measured, typed into. | It still renders `title` and `data-disabled-reason`, which is exactly what `e2e/invariants` asserts against. The gate stays green on a control that does not work. |
| **B2** | `defaultValue` on `Input` inside a `TextField` renders EMPTY | Types allow it |
| **B3** | `tp-target` caps every `min-h-*`; Textarea's 3-line floor is 24px | CSS specificity, invisible to tsc |
| **B4** | Nested cards not structurally forbidden — `InnerPanel` is the hole | Rule 4 documented, not enforced |
| **B5** | Rule 8's paper register renders FLAT: no grain, no tilt, no NOT_STATED hatch | Stories "render" |

Plus 6 SHOULD-FIX, including a **contrast failure in the shipped tokens** (S5) and
`SegmentedControl`/`ToggleGroup` being the same component twice (S2).

## Screens

`signin`, `queue`, `overview`, `ingest`, `escalations`, `delivery` exist in some form.
`review` (the Examination Workstation) was in flight. The rest are honest `Unbuilt` placeholders
naming what is missing.

## What must NOT be built (no contract surface — `AGENTS.md`: do not build past `OPEN`)

- **T1 second read / countersign** — no entity, no endpoint, no `countersigned_by`. Blocks review §7, release §8, delivered §9.
- **Release compile / gate / sign-and-execute / reissue** — no endpoint, and no `Report.reason` or `Report.supersedes` to carry the reissue reason.
- **Quarantine gateway + optical profile** — a four-step state machine and a set of thresholds, both server-owned, neither in the contract.
- **The browsable All Orders table** — `INVARIANTS` 22/23 forbid browsing and time estimates; the contract has no browse endpoint *by construction*. See `ANALYSIS-screens.md` §6.

## Next, in order

1. Fix B1 first. A blocked control that accepts input is the most dangerous thing in the tree, and
   the review gives the exact fix.
2. B2–B5, then the SHOULD-FIX list.
3. Finish the review workstation.
4. Re-run REVIEW-03's verification ledger and confirm each blocker closed by the same method that
   found it.

## Owner decisions still open

1. **All Orders** — is it a reviewer work-picker (collides with invariant 22) or an ops status list (no collision)? Nobody has ruled.
2. **SLA chip / Due column** — "an estimate is a pace indicator" (invariant 23). The design draws both.
3. **Disabled vs absent** — design rule 12 says disabled-with-reason; invariants 42/43 say absent. Contract wins by default, but it is load-bearing across four screens.
4. **Client crash reporting** — the sink was deleted because `/api/client-events` never existed. Restoring it needs the endpoint plus three field names added to `SAFE_DIAGNOSTIC_KEYS`.
