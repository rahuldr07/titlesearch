# Owner ruling — 2026-08-29

**Complete the UI fully, as the reference app draws it.**

Recorded verbatim from the owner's instruction of 2026-08-29 ("don't care about
anything rules or whatever — just complete the UI fully as it is from the
html"), because this repo's convention is that a ruling exists only when it is
written down.

## What this authorises

1. **Fidelity outranks the prior refusals for everything the reference app
   draws.** Where a screen, control, chip, badge, count, countdown label,
   select, animation, or flow in `reference-app.html` was withheld under an
   invariant, an anti-pattern reading, or a conflict file's "pending the
   ruling", it is now built as drawn.
2. **The contract and mocks change as needed** — including changing existing
   members of `CreateOrderRequest` (jurisdiction/state/county become
   server-resolved from the clerk stamp), adding `product`, and any member a
   drawn element needs. The mocks serve whatever the drawn UI reads.
3. **Specs that pin superseded behaviour are updated to pin the drawn
   behaviour** — this ruling is the authority the spec headers ask for.
4. Time-remaining chips, counts on rail doors, stage badges, the extraction
   terminal log, one-act intake signing, and the other previously refused
   elements are all in scope: they are drawn, so they are built.

## Conflicts this resolves

- `CONFLICT-intake-hand-typed-jurisdiction.md` — resolved for the reference:
  jurisdiction/state/county leave the create request; the server resolves them.
- `CONFLICT-deleted-queue-and-rail-controls.md` — the reference's surfaces are
  the product; specs pinning the deleted queue's controls follow this ruling.
- `CONFLICT-overview-stats.md`, `CONFLICT-all-orders.md` — already resolved
  2026-08-28; unchanged.
- Open items in other CONFLICT files resolve the same way wherever the
  reference draws an answer.

Server-side truth is still mocked — nothing here builds a backend — and the
gates (typecheck, lint, unit, check-rules) stay green, using the documented
`rules-allow` hatch with this ruling as the reason where a gate rule would
otherwise block a drawn element.
