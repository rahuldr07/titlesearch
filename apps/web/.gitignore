# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
# The live migration harness's bundles. Kept OUT of dist/ because `pnpm build`
# empties that directory wholesale and was measured deleting them mid-run.
dist-harness
*.local

# build artefacts
storybook-static
test-results
playwright-report
.tmp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Reviewer scratch — probe files, throwaway builds. Agents write these to prove
# a claim by executing it rather than asserting it; none of it is source.
*probe*
dist-probe/
