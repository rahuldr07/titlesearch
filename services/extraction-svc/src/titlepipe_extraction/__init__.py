"""TitlePipe extraction worker.

Document validation, segmentation, classification, extraction, assembly,
validation and routing. Runs as its own process with its own credentials; it
never serves HTTP.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
