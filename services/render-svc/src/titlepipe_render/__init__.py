"""TitlePipe render worker.

DOCX generation, Gotenberg conversion, report versioning and delivery
preparation. Runs as its own process with its own credentials; it never serves
HTTP.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
